{
  "buildCommand": "cross-env NODE_ENV=production NODE_OPTIONS=--no-warnings webpack --config web/webpack.config.js --mode production",
  "outputDirectory": "web/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
